language: "id"
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<nyala_imperatif> <name> [di <area>]"
        excludes_context:
          domain:
            - binary_sensor
            - cover
            - lock
            - scene
            - script
            - sensor
            - valve
