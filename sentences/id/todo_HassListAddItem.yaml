language: "id"
intents:
  HassListAddItem:
    data:
      - sentences:
          - tambah[kan] <item> ke<my_list>
        response: item_added
        requires_context:
          domain: todo
        expansion_rules:
          my_list: "[ daftar] {name}"
          item: "{shopping_list_item:item}"
